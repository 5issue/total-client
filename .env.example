# ─────────────────────────────────────────────────────────────
# 브라우저에 노출됨 (NEXT_PUBLIC_ 접두어)
# ⚠️ 빌드 타임에 번들로 인라인된다. 배포 후 ConfigMap 을 바꿔도 반영되지 않는다.
#    값을 바꾸려면 이미지를 다시 빌드해야 한다 (Dockerfile builder ARG).
# ─────────────────────────────────────────────────────────────
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_APP_ENV=local
# 토스페이먼츠 클라이언트 키(브라우저 노출 OK). 시크릿 키를 넣지 않는다.
# 반드시 API 개별 연동 키(`test_ck_`)를 쓴다 — 이 앱은 결제수단 선택 UI를 이미 자체
# 그리드(PaymentMethodAccordion)로 구현했다. 위젯 키(`test_gck_`)를 넣으면
# `requestTossCheckoutPayment` 가 토스 결제위젯(자체 선택 UI 포함)을 띄워서, 이미 고른
# 결제수단(토스페이 등) 위에 토스 쪽 선택 화면이 또 겹쳐 보인다("다음을 눌러주세요" 대신
# 우리 그리드와 똑같이 생긴 토스 위젯이 다시 뜨는 버그의 원인).
# 아래 값은 토스 문서용 API 개별 연동 테스트 키. 상점 키로 교체한다.
NEXT_PUBLIC_TOSS_CLIENT_KEY=test_ck_D5GePWvyJnrK0W0k6q8gLzN97Eoq

# ─────────────────────────────────────────────────────────────
# 서버 전용 (NEXT_PUBLIC_ 접두어 없음)
# 브라우저 번들에 포함되지 않는다. 런타임에 K8s ConfigMap/Secret 으로 주입 가능.
# ─────────────────────────────────────────────────────────────
API_INTERNAL_URL=http://localhost:4000

# AI 파트 서빙(FastAPI, Spring 과 별도 호스트) 내부 URL — My냉장고/레시피 추천 등(#138).
# 로컬은 AI 레포(5issue/KDT-2-AI-Integrated-Project-Team5-AI) serving/README.md 안내대로
# docker build/run 후 8000 포트로 뜬다.
AI_SERVICE_INTERNAL_URL=http://localhost:8000

# 로컬 전용. Spring 백엔드 미배포 구간을 MSW 로 목킹할지 여부 — "enabled" 로 설정하면
# Route Handler 가 호출하는 외부 API 를 MSW 가 가로챈다(src/instrumentation.ts). 빈 값이면 항상 꺼짐.
API_MOCKING=

# 로컬 전용. 실기기(폰)에서 같은 네트워크의 맥/PC IP로 dev 서버 접속 테스트할 때만
# 설정한다(`ipconfig getifaddr en0` 등으로 확인). 비워두면 LAN 접속이 꺼진다 —
# IP가 달라 값을 공유하지 않는다.
DEV_LAN_IP=
