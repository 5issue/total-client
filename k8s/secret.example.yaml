# 자리표시(placeholder). 실제 Secret 은 커밋하지 않는다.
# SealedSecrets / External Secrets Operator / SOPS 등으로 주입할 것.
apiVersion: v1
kind: Secret
metadata:
  name: total-client-secret
  labels:
    app: total-client
type: Opaque
stringData:
  # 서버 전용 민감값 자리표시 (NEXT_PUBLIC_ 접두어 금지 — 브라우저 노출 방지).
  API_INTERNAL_TOKEN: "replace-me"
